server {
    listen 80;
    server_name mushapp.dev www.mushapp.dev;
    
    # HTTP'yi HTTPS'e yönlendir (Sertifika kurulunca açılacak)
    # return 301 https://$host$request_uri;
    
    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /app/staticfiles/;
    }

    location /media/ {
        alias /app/media/;
    }
}

# SSL bloğu (Sertifika dosyaları gelince aktif edeceğiz)
# server {
#     listen 443 ssl;
#     server_name mushapp.dev www.mushapp.dev;
#
#     ssl_certificate /etc/nginx/certs/mushapp.crt;
#     ssl_certificate_key /etc/nginx/certs/mushapp.key;
#
#     location / {
#         proxy_pass http://web:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
